<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.fdev.unisaconnect"
    android:installLocation="auto"
    android:versionCode="40"
    android:versionName="0.7.5" >

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="20" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_LOGS" />
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="false"
        android:hardwareAccelerated="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/MyTheme_Orangy" >
<!--         <provider -->
<!--             android:name="it.fdev.unisaconnect.MySuggestionProvider" -->
<!--             android:authorities="it.fdev.unisaconnect.MySuggestionProvider" /> -->

<!--         <meta-data -->
<!--             android:name="android.app.default_searchable" -->
<!--             android:value="it.fdev.unisaconnect.MainActivity" /> -->

        <!-- android:screenOrientation="fullUser" -->
        <!-- android:screenOrientation="userPortrait" -->
        <activity
            android:name="it.fdev.unisaconnect.MainActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:screenOrientation="userPortrait"
            android:windowSoftInputMode="adjustResize|stateHidden" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
<!--             <intent-filter> -->
<!--                 <action android:name="android.intent.action.SEARCH" /> -->
<!--             </intent-filter> -->
<!--             <intent-filter> -->
<!--                 <action android:name="android.intent.action.VIEW" /> -->
<!--             </intent-filter> -->

<!--             <meta-data -->
<!--                 android:name="android.app.searchable" -->
<!--                 android:resource="@xml/searchable" /> -->
        </activity>

        <receiver android:name="it.fdev.unisaconnect.wifilogin.NetworkStateChanged" >
            <intent-filter>
                <action android:name="android.net.wifi.STATE_CHANGE" />
            </intent-filter>
        </receiver>
        <receiver android:name="it.fdev.utils.MusicIntentReceiver" >
            <intent-filter>
                <action android:name="android.media.AUDIO_BECOMING_NOISY" />
            </intent-filter>
        </receiver>

        <service
            android:name="it.fdev.unisaconnect.wifilogin.LoginManager"
            android:exported="false" >
        </service>
        <service
            android:name="it.fdev.scraper.esse3.Esse3ScraperService"
            android:exported="false" >
        </service>
        <service
            android:name="it.fdev.scraper.BiblioSearchScraperService"
            android:exported="false" >
        </service>
        <service
            android:name="it.fdev.unisaconnect.WebRadioPlayerService"
            android:exported="false" >
        </service>

        <!-- Start of Crittercism.com Code -->
        <activity android:name="com.crittercism.NotificationActivity" />
        <!-- End of Crittercism.com Code -->
    </application>

</manifest>